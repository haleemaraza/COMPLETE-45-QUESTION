"use strict";
// storge and print names
let names = ["Alif", "Aylin", "Haya", "Arfa"];
// print each person name 
for (let name of names) {
    console.log(name);
}
;
// greeting
for (let name of names) {
    console.log(`Hello ${name}, lets meets again... `);
}
;
