
function showMagicians_(magicians: string[]): void {
    magicians.forEach((magician) => {
      console.log(magician);
    });
  }
  
  // magician names
  const magicianNames_: string[] = ["Harry potter", "Albus Dumbledore", "Hermione", "Ron "];
  

  showMagicians_(magicianNames_);